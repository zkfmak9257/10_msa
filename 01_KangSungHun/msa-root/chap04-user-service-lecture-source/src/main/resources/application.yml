#spring.application.name=chap04-user-service-lecture-source
server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: swcamp-user-service
  config:
    import:
      - classpath:/bootstrap.yml
#  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
#    url: jdbc:mariadb://localhost:3306/cqrs
#    username: swcamp
#    password: swcamp
#  jpa:
#    hibernate:
#      ddl-auto: create
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true


mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mappers/**/*.xml
  type-aliases-package: com.kang.userservice.query.dto

# Config Server로 설정 이동
#jwt:
#  secret: LH9QZL8upsPBfuDY+Dkb1kT9DZIIUSuA2u4O6Lfi3mkEfeWtETpVTcR/8SMZdJWn/xNTuCQBE6rBvDXgnVmscQ==
#  expiration: 1800000
#  refresh-expiration: 604800000
#

### 엔드포인트의 노출 여부 제어
management:
  endpoints:
    web:
      exposure:
        include: health, info, beans, httpexchanges, metrics



